using System.Security.Cryptography;
using UnityEngine;

public class RangedUp : MonoBehaviour
{
    public Upgrades rup;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    
}
